<?php

/**
 * @file
 * Contiene funciones personalizadas para mi módulo de registrar usuarios.
 */

use Drupal\Core\Database\Database;

/**
 * Implements hook_block_info().
 */
function registro_usuario_block_info() {
  $blocks['registro_usuario_bloque'] = [
    'info' => t('Mi formulario de registro de usuarios'),
    'cache' => DRUPAL_NO_CACHE,
  ];
  return $blocks;
}